let username='Akshata'
console.log(username);

//tsc main.ts for compilation of javascript file from typescript file
//tsc main.ts -w : We can continue to watch changes that we have done in typescript file
//tsc --init to initialize the typrscript.it will create tsconfig.jsdon
//"include": [  "src" ] addition of this in tsconfig.json will  ignore the ts file outside the src and will not compile to javascript file

let a: number =12;
let b:number=6;
let c: number=2;
console.log(a/b)

console.log(c*b)